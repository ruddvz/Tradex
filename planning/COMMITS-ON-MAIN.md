# Recent commits on `main` (quick reference)

If a GitHub PR shows **“no commits”** or **no diff**, it usually means the branch was **fast-forward merged** into `main` (the commits live on `main`, not as an open PR diff). Use **Commits** on `main` or compare SHAs below.

| SHA | Message |
|-----|---------|
| `5c4d25e` | docs: record Plan0 polish commit sha in CHANGELOG |
| `2818932` | feat(frontend): Plan0 completion — lazy routes, suspense boundaries, nav a11y |
| `cdc1c63` | docs: record commit sha in CHANGELOG for slice 1.1 |
| `95b72e3` | feat(auth): JWT users table, Bearer protection, user-scoped stores |
| `84ad2a2` | docs: record merge commit sha in CHANGELOG |
| `cf5dbc6` | Merge branch `cursor/plan0-pixel-perfect-ui-66d8` into phase0 consolidation |
| `ff873e6` | docs: complete Phase 0 architecture skill and Plan0 workflow link |
| `5e78eb4` | feat(frontend): finish Plan0 — boot skeleton, audit doc, safe areas |
| `4a078ca` | feat(frontend): implement Plan0 pixel-perfect PWA UI and interactions |

**See also:** `planning/PHASE0-INVENTORY.md`, `planning/CHANGELOG.md` (newest entry first).

Open feature work should use a branch such as `cursor/<topic>-07ef` and a PR **before** fast-forward merging to `main` if you need a visible PR timeline.
